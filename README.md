Выбор фреймворка для создания микрофронтендов

Проанализировав структуру проекта я выделил следующие особенности:
1. Основная технология, использованная для написания фронтенда - React (17.0.2)
2. Используется редирект по URL для Log in и Sign up
3. Некоторые копоненты переиспользуются несеолько раз. Например, PopupWithForm используется во всех компонентах с поп-апами (AddPlacePopup, EditAvatarPopup и тд)
4. В App.js создаются общие переменные состояния, такие как currentUser и email
5. В приложении используется общий слой с утилитами. API и auth используются всеми компонентами

Вывод:
Для разбиения приложения на микрофронтенды в данном случае больше подходит фреймворк Module Federation. Он хорошо зарекомендовал себя в случаях когда:
1. В приложении используется единый технический фреймворк. В данном случае приложение Mesto использует только React.js
2. Переиспользуются компоненты и модули. Mesto использует общий api и auth слой. Некоторые компоненты ссылаются на другие (как, например с Pop-up)
Также Module Federation позволит оптимизировать загрузку библиотек и динамически подгружать требуемые микрофронтенды. Это сэкономит ресурсы и оптимизирует бандл. 
Из минусов, Module Federation не очень хорошо оптимизирован для маршрутизации и разделения приложения на полностью независимые микрофронтенды. Но, в Mesto нет сложной маршрутизации, а полной независимости микрофронтендов не требуется из-за изначальной связанности компонентов. Также, при необходимости, маршрутизацию можно реализовать при помощи SSR.
Исходя из вышеперечисленного наиболее подходящим фреймворком для приложения Mesto является Module Federation

=================================================================================================================================================================

Разделение на микрофронтенды

Исходный монолит разбит на микрофронтенды по принципу вертикальной нарезки. Каждый микрофронтенд отвечает за свою отдельную бизнес-функцию. Например, компонент auth-microfrontend отвечает за логин и регистрацию пользователя, тогда как profile-microforntend отвечает за работу с профилем пользователя. Компоненты содержат необходимые утилиты и стили для независимой работы. Общие стили вэб-страницы находятся в компоненте host. Они отвечают за общее отображение контента, секций и страниц.
В отдельный компонент вынесен функционал работы с "картами" и тултипами, так как они унифицированы по приложению и могут работать независимо от остальных микрофронтендов.
Так как данное приложение обладает довольно сложным пользовательским интерфейсом, которые обслуживают разные бизнес-функции, данный подход выглядит наиболее подходящим.

Структура приложения:

/host-microfrontend                 // Микрофронтенд, который хостит и оркестрирует остальные
    /src
        /components
            App.js                  // Основной компонент, в который импортируются все микрофронтенды
            Footer.js               // Компонент футера
            Header.js               // Компонент хэдера
            Main.js                 // Main компонент
            ProtectedRoute.js       // Роутинг компонент
        /styles
            /page                   // Папка со стилями для страницы
            /content                // Папка со стилями контента
        /utils
            api.js                  // Утилиты для API
        /contexts
            CurrentUserContext.js    // Файл с контекстом
        index.js                      // Точка входа микрофронтенда
    package.json                    // Зависимости и скрипты микрофронтенда
    webpack.config.js

/auth-microfrontend
    /src
        /components
            Login.js                // Компонент входа пользователя
            Register.js             // Компонент регистрации пользователя
        /styles
            login.css               // Стили для компонента входа
            /auth-form              // Папка со стилями для формы аутентификации пользователя
        /utils
            auth.js                 // Утилиты для аутентификации
        index.js                    // Точка входа микрофронтенда
    package.json                    // Зависимости и скрипты микрофронтенда
    webpack.config.js

/profile-microfrontend 
    /src
        /coponents
            EditAvatarPopup.js      // Компонент изменения аватара пользователя
            EditProfilePopup.js     // Компонент изменения профиля пользователя
            PopupWithForm.js        // Компонет, отображающий поп-апы
        /styles
            /profile                // Папка со стилями для компонента профайла
            /popups                 // Папка со стилями для поп-апов
        /utils
            api.js                  // Утилиты для API
        index.js                    // Точка входа микрофронтенда
    package.json                    // Зависимости и скрипты микрофронтенда
    webpack.config.js

/card-microfrontend
    /src
        /components
            Card.js                 // Компонент с картами
            ImagePopup.js           // Компонент с изображением для карты
            PopupWithForm.js        // Компонент с поп апом
        /styles
            /card                   // Папка со стилями для карт
                ..card.css          // Стили для карт
            /popup                  // Папка со стилями для поп-апов
                ..popup.css         // Стили для попапов
        /utils
            api.js                  // утилиты для API
      index.js                      // Точка входа микрофронтенда
    package.json                    // Зависимости и скрипты микрофронтенда
    webpack.config.js

/info-microfrontend
    /src
        /components
            InfoTooltip.js          // Компонент с тултипом
        /styles
            /popup                  // Стили для тултипа. Используются классы из поп-апов
      index.js                      // Точка входа микрофронтенда
    package.json                    // Зависимости и скрипты микрофронтенда
    webpack.config.js



/place-microfrontend
    /src
        /coponents
            AddPlacePopup.js        // Компонент для добавления места
            PopupWithForm.js        // Компонент, отображающий поп-апы
        /styles
            /places                 // Папка со стилями для компонента профайла
            /popups                 // Папка со стилями для поп-апов
        index.js                    // Точка входа микрофронтенда
    package.json                    // Зависимости и скрипты микрофронтенда
    webpack.config.js

